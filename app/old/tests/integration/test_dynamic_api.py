class TestDynamicAPI:
    """
    TODO: Setup app with active config files to ensure build results in
    expected app with correct API objects/endpoints/spec.
    """

    # @pytest.fixture
    # def builder(self):
    #     builder = DynamicAppBuilder(TestSettings.BASE_TEST_CONFIG_FILE)
    #     builder._load_yml = MagicMock(return_value={})
    #     return builder
